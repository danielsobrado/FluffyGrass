export const GRASS_IMPOSTOR_SUBPATCHES_PER_AXIS = 2;
export const GRASS_IMPOSTOR_MAX_ATLAS_SIZE = 2048;
/**
 * Keeps neighbouring atlas frames isolated through the mip range where the
 * runtime still permits stochastic alpha coverage.
 */
export const GRASS_IMPOSTOR_MIN_PADDING = 4;
